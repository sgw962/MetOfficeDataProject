from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Weather forecasting project based on met weather data',
    author='Sean',
    license='',
)
